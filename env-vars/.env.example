# Unica variavel lida pela CLI: LOG controla o nivel de log.
# Valores tratados: "verbose" (DEBUG) e "quiet" (INFO). Qualquer outro usa o padrao (WARNING).
# Ver src/config/setup_log.py.
LOG="verbose"
