import 

# mockar o requests.get -> pra retornar o que vocês desejam
def test_list_request(self, status_code,mocker)
    with mocker.patch(
        "requests.get",
        return_value = 
    )
""" mocker.patch(
        "requests.get",
        return_value={"characteristics": ["Usability", "Maintainability"]},
    ) """

# chamar a função que vocês desejam testar

# mockar o sys.out -> pra ver o que o programa imprime

# with mocker.patch("sys.stdout", new=StringIO()) as fake_out: 

# fazer os asserts baseados no que vocês esperam que a função retorne